language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t anvargzv/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true anvargzv/docker-react npm run test -- --coverage
